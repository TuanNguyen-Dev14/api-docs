# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-09-22T10:03:44.903Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  workflow_dispatch:
    inputs:
      version:
        description: Input ypur Open API version
        required: true
        type: string

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi public.json --version=${{ inputs.version }} --key=${{ secrets.README_API_KEY }} --id=650d659072fda90013aec66c
