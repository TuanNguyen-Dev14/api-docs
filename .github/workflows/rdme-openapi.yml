# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-09-22T14:10:29.739Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  workflow_dispatch:
    inputs:
      api-definition-id:
        description: Input your API definition id
        required: true
        type: string

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi public.json --key=${{ secrets.README_API_KEY }} --id=${{ inputs.api-definition-id }}
